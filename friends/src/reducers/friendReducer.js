const initialState = {
  friends: [],
  isLoading: false,
  error: ""
};

export const friendReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
